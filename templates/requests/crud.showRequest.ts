import {
    IsDefined
} from 'class-validator';

export class templateNameShowRequest {
    // @IsDefined()
    // email!:     string;

    constructor(request) {
       // this.email      = request?.body?.email;
    }
}