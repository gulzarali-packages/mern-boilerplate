import {
    IsDefined
} from 'class-validator';

export class templateNameCreateRequest {
    // @IsDefined()
    // email!:     string;

    constructor(request) {
       // this.email      = request?.body?.email;
    }
}