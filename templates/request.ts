import {
    IsDefined
} from 'class-validator';

export class templateNameRequest {
    // @IsDefined()
    // email!:     string;

    constructor(request) {
       // this.email      = request?.body?.email;
    }
}